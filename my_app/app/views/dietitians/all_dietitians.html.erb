<div class="page-header">

<div class="container">
  <div class="jumbotron">
    <h1>Meet Our Dietitians</h1> 
    <p>We have the best registered dietitians from all around the word and with many different expertise</p> 
  </div>
</div>
</div>

<!-- <div class="row">
  <div class="col-md-12">

<div class="row">
<% @dietitians.each do |dietitian| %>
  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
      <img src=<%= dietitian.image %> height="100" width="121">
      <div class="caption">
        <h3><%= dietitian.name %></h3>
        <p><%= dietitian.areas_of_expertise %></p>
        <p>
        <a href="/diet/<%= dietitian.id %>" class="btn btn-primary">Show</a>
          <% if current_dietitian == dietitian %>
            <a href="/diet/edit_dietitian/<%= dietitian.id %>" class="btn btn-warning">Edit</a>
          <% end %>
        </p>
      </div>
    </div>
  </div>
  <% end %>
</div>
 -->


    <table class="table table-striped table-hover">
      <tr>
        <th>Image</th>
        <th>Dietitian</th>
        <th>Email</th>
        <th>Areas of expertise</th>
        <th>Action</th>
      </tr>

      <% @dietitians.each do |dietitian| %>
      <tr>
        <td>
          <% if dietitian.image != nil %>
            <img src="<%= dietitian.image %>" width="100">
          <% else %>
            <img src="http://localhost:3000/assets/client.jpg" width="100">
          <% end %>
        </td>
        <td><%= dietitian.name %></td>
        <td><%= dietitian.email %></td>
        <td><%= dietitian.areas_of_expertise %></td>
        <td>
          <a href="/diet/<%= dietitian.id %>" class="btn btn-primary">Show</a>
          <% if current_dietitian == dietitian %>
            <a href="/diet/edit_dietitian/<%= dietitian.id %>" class="btn btn-warning">Edit</a>
          <% end %>
        </td>
      </tr>
      <% end %>
    </table>

  </div>
</div>